upload_max_filesize=40M
post_max_size=40M

; Disable OPcache in development for better code sync
opcache.enable=0
; Or if you want to keep it enabled but revalidate on every request:
; opcache.enable=1
; opcache.revalidate_freq=0

